/**
 * 返回 hello 开头的字符串
 * @param str - input string
 * @returns 'hello xxx'
 * @example
 * ```ts
 * test('ts') => 'hello ts'
 * ```
 *
 * @beta
 * @author ziming
 */
export const a = (test: string): string => {
    return 'hello' + test
}
